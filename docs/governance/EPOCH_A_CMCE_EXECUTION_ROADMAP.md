# Epoch A — CMCE Proliferation & Hardening
## Optimized Execution Roadmap (Track A → Full Track B)

**Current Date:** 2026-05-28  
**Current Authority:** Track A (aggressive implementation + artifact preparation)  
**Blocking Gates:** Phase 198 GPG on ADAADell + four-surface reconciliation

### Phase 199 — CMCE Core Integration Foundation (Current Focus)
**Goal:** Make CMCE the non-bypassable gate in the canonical mutation path.

**Completed (Aggressive Track A execution):**
- Real adapter to `ConstitutionalMutationConsensusEngine` (fixed + production-grade).
- `propose_mutation_with_cmce` + enforcement inside `EvolutionKernel.run_cycle`.
- Provisional mode for foundation phase (PENDING → PROVISIONAL).
- New Hard invariants: CMCE-GATE-0, CMCE-EXEMPT-0.
- Full governance artifact set + migration guide.
- Expanded tests + performance guard.
- CMCE correlation attached to every cycle event.

**Remaining in Phase 199:**
- End-to-end test harness with simulated voting (so we can get real PASSED outcomes).
- CMCE event emission standardization.
- Update all internal call sites to prefer the new path.

**Target:** Ready for merge the moment Phase 198 gates close.

### Phase 200 — DORK + CMCE (High Leverage Next)
**Goal:** Force significant DORK outputs through CMCE.

**Pre-work started:**
- `runtime/dork/cmce/dork_cmce_adapter.py` stub created.
- Integration points documented.

**Planned:**
- Wire DORK proposals/insights through the adapter.
- New DORK-CMCE invariants.
- Ledger correlation for DPM/DQR/etc.

### Phase 201 — SPIE Hardening
- Self-proposed innovations must pass CMCE.

### Phase 202 — Stress Testing & Retrospective
- Large-scale historical replay through CMCE.
- First CMCE-mediated audit of Phases 100–198.

### Cross-Cutting Optimizations (Ongoing)
- CMCE correlation ID standard across all ledgers and events.
- Performance & determinism guards.
- Clean provisional → strict transition flag.
- Comprehensive bypass inventory and elimination.

### Gate Closure Dependency
Everything above is being built so that once HUMAN-0 completes Phase 198 GPG on ADAADell and the four surfaces are reconciled, the entire Epoch A stack can be activated with minimal additional changes.

**"Best Path" Principle:** Build the constitutional substrate first (199), then wire the highest-impact consumers (DORK 200, SPIE 201), then prove it at scale (202).

This roadmap is being actively executed under Track A. Full activation requires gate closure.