# ADAAD v9.106.0 — Phase 172

- Version alignment release note for Phase 172.
- Syncs the complete product-version surface set to canonical `VERSION` (`9.106.0`), including `adaad.__version__`, agent-state release fields, and governance report version fields.
- Preserves `schema_version` fields as schema compatibility metadata, not product release metadata.
