# archives

Archives contain historical snapshots and imported bundles (like initial zips).
They are not part of the active import graph.

Rules:
  - do not import modules from archives/
  - treat archives as read-only history
  - extract and migrate only through the Architect process
