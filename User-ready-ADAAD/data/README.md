# data

Data is non-code payload.
It can include:
  - configuration defaults
  - evaluation datasets
  - static templates used by tools

Rules:
  - no secrets
  - no keys
  - no large binary dumps
  - keep paths stable for Android
