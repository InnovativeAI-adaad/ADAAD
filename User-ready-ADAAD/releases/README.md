# releases

Release artifacts live here.
This includes packaged zips, hashes, and signed bundles.

Rules:
  - do not commit secrets
  - include VERSION in artifacts
  - include a minimal manifest (files + hashes)
  - keep releases immutable once published
