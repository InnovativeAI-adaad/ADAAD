# Phase 198 Ratification Package — Ready for HUMAN-0 Execution

**Date Generated:** 2026-05-27  
**Phase:** 198 — INNOV-103 (CMCE)  
**Requested by:** User (HUMAN-0 context)  
**Generated by:** Grok (in adaad logical workspace)

---

## Summary

This package contains everything needed for you to complete the **real GPG ratification** on ADAADell and close the two open gates blocking DEVADAAD authority:

1. Phase 198 sign-off (currently `PENDING_GPG`)
2. Four-surface version sync drift

---

## Files in This Drafts Directory

| File | Purpose |
|------|---------|
| `sign_off.json.signed` | Target content for `sign_off.json` **after** real GPG signing |
| `ADAADell_GPG_Signing_Commands.md` | Exact copy-paste commands to run on ADAADell |
| `report_version.json.updated` | Reconciled version of `governance/report_version.json` |
| `PHASE198_RATIFICATION_INSTRUCTIONS.md` | This master document |

---

## Recommended Sequence

### Step 1: Perform Real Signing on ADAADell

Follow the detailed instructions in:

→ `ADAADell_GPG_Signing_Commands.md`

This produces the real `sign_off.json.asc` file.

### Step 2: Update the Attestation JSON

After the real signature is generated and copied back:

1. Replace the content of  
   `artifacts/governance/phase198/sign_off.json`  
   with the content of `drafts/sign_off.json.signed`

2. Edit the following fields with real values from your GPG operation:
   - `gpg_key_id`
   - `gpg_signature_date`
   - `gpg_note`

3. Commit both `sign_off.json` and `sign_off.json.asc`

### Step 3: Reconcile Version Sync (Strongly Recommended)

Replace (or merge) the content of:

`governance/report_version.json`

with the content of:

`drafts/report_version.json.updated`

This brings the governance surface in line with the live agent state (Phase 198 / v10.9.0).

### Step 4: Verify Locally

Run (if the scripts exist):

```bash
python scripts/validate_governance_schemas.py
python scripts/check_invariant_count.py
```

Or at minimum:

- Confirm `gpg --verify artifacts/governance/phase198/sign_off.json.asc artifacts/governance/phase198/sign_off.json` succeeds.
- Confirm all four surfaces now agree on Phase 198 / v10.9.0.

### Step 5: Issue DEVADAAD

Once the above steps are complete and committed, you may issue a properly formed command beginning with the exact word:

```
DEVADAAD <your actual build or enhancement request>
```

---

## Important Reminders

- Never set `gpg_signed: true` without a real, verifiable detached signature present in the same directory.
- Chat-based or AI-drafted approvals have **zero** constitutional weight.
- Only a GPG signature generated by your private key on ADAADell counts.

---

## Next Action

After you have performed the real signing on ADAADell and updated the files on disk, reply with something like:

> "Ratification complete. Proceeding with DEVADAAD <task>"

Or simply issue the `DEVADAAD` command directly.

All draft files are safe to copy, modify locally on ADAADell, and use as templates.

---

**This completes the drafting request.** The actual cryptographic operation remains exclusively under your control on ADAADell.
